x <- 10
sqrt(x)  # caclula raiz quadrada
factorial(x)  # calcula o factorial
factorial(10)

y <- -6
abs(y)  # retorna o valor absoluto de um numero
log(x)  # calcula logaritmo de x na base 10 (por defeito)
log(x, base = 2)  # calcula o logaritmo de x na base 2

c <- log(x, base = 2)
c  # variavel 'c' com 6 casas decimais
# funcao de aredondamento de um numero
round(c, digits = 2) # mostra a variavel 'c' com duas casas decimais
