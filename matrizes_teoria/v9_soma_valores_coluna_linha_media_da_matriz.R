# Soma todos os elementos contidos em cada linha da matriz.
print(rowSums(A))

# Soma todos os elementos contidos em cada colun da matriz.
print(colSums(A))

# Computa a média dos elementos de cada linha da matriz
print(rowMeans(A))

# Computa a média dos elementos de cada coluna da matriz
print(colMeans(A))